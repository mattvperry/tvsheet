import { doubleFn } from './double';

/**
 * Double a number
 *
 * @param {number} n number to double
 * @returns doubled number
 * @customfunction
 */
function DOUBLE(n: number): number {
  return doubleFn(n);
}
